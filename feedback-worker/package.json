{
  "name": "draftroom-feedback-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply draftroom-feedback --local",
    "db:remote": "wrangler d1 migrations apply draftroom-feedback --remote",
    "check": "node --check src/worker.js && node --check ../feedback/reader.js",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
